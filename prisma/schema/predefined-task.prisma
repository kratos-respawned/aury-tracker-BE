model PredefinedTask {
  id          String   @id @default(cuid())
  name        String
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // Relations
  schedules PredefinedTaskSchedule[]
  tasks     Task[]

  @@map("PredefinedTask")
}
