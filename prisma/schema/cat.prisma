model Cat {
    id        String    @id @default(cuid())
    name      String
    gender    String
    birthday  DateTime?
    breed     String?
    createdAt DateTime  @default(now())
    updatedAt DateTime  @updatedAt

    @@map("cat")
}
